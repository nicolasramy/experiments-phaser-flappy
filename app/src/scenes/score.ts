import { Scene } from 'phaser';

export default class ScoreScene extends Scene {
  constructor() {
    super({
      key: 'ScoreScene'
    });
  }

  preload(): void {
    //
  }

  create(): void {
  }
}
